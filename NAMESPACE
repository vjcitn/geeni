import(methods)
import(ff)
import(IRanges)
import(BiocParallel)

exportClasses(gdManager)

exportMethods(getGrp, formula)

export(pargee)

export(
    getGrp, gd2flat, getDep, getEta, getMu, getX, getY, Di,
    Vinv.i, ri, Gcomps, combi,
    discrim, numdat 
)
